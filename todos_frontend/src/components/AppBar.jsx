import React from 'react';

const AppBar = () => {
  return (
    <header className="app-bar">
      <h1>Todo List</h1>
      <button className="theme-toggle">
        Toggle Theme
      </button>
    </header>
  );
};

export default AppBar;
